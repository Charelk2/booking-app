# Backend production environment

# REQUIRED: set a strong, unique secret
SECRET_KEY=change_me_in_prod
ALGORITHM=HS256

# Database and Redis (examples)
# SQLALCHEMY_DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST:5432/DBNAME
# REDIS_URL=redis://default:password@redis:6379/0

# Frontend base URL (used for OAuth redirects, WebAuthn origin/RP)
FRONTEND_URL=https://booka.co.za

# CORS configuration: allow your site origins
CORS_ORIGINS=["https://booka.co.za","https://www.booka.co.za"]
CORS_ALLOW_ALL=false

# Email delivery settings (disable dev echo of links)
EMAIL_DEV_MODE=false
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=no-reply@booka.co.za

# OAuth (Google login)
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=

# Google Calendar OAuth (calendar sync)
# Use the same Client ID/Secret as configured in Google Cloud (or separate credentials).
# Ensure Authorized redirect URIs in Google console include:
#   - https://api.booka.co.za/api/v1/google-calendar/callback
#   - http://localhost:8000/api/v1/google-calendar/callback (for local dev)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://api.booka.co.za/api/v1/google-calendar/callback
