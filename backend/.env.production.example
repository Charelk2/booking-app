# Backend (Render) production env template

# Switch to Postgres for production (example URL below)
# postgresql+psycopg://USER:PASSWORD@HOST:PORT/DBNAME
SQLALCHEMY_DATABASE_URL=

# Redis for caching (optional but recommended)
# redis://USER:PASS@HOST:PORT/DB
REDIS_URL=

# Required for sessions and JWTs
SECRET_KEY=

# Frontend base URL (for OAuth callbacks/redirects)
FRONTEND_URL=https://booka.co.za

# CORS configuration for your domains
CORS_ORIGINS=["https://booka.co.za","https://www.booka.co.za"]

# Optional: allow additional attachment hosts (comma-separated) for the
# attachments proxy/redirect endpoint. Leave unset unless you serve attachments
# from a custom domain in addition to '*.r2.cloudflarestorage.com'.
# Example: ATTACHMENTS_PROXY_ALLOWED_HOSTS=media.booka.co.za
ATTACHMENTS_PROXY_ALLOWED_HOSTS=

# Optional: cap the size (MB) of responses when streaming via
# /api/v1/attachments/proxy?mode=stream. Redirect mode is unaffected.
# Set 0 to disable. Default 50.
ATTACHMENTS_PROXY_MAX_MB=
CORS_ALLOW_ALL=false

# OAuth / SMTP (optional)
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=no-reply@booka.co.za
