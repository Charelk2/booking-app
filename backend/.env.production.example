# Backend (FastAPI) production env template

# Core app
SECRET_KEY=<generate-a-strong-secret>
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Database (configure your production DB URL)
SQLALCHEMY_DATABASE_URL=sqlite:////data/booking.db

# CORS + frontend origin
FRONTEND_URL=https://booka.co.za
# Scope cookies across site + API on subdomains (do not include attributes):
COOKIE_DOMAIN=.booka.co.za

# Redis (recommended)
REDIS_URL=redis://default:<password>@<host>:6379/0

# WebSocket Redis for cross-process fanout + SSE fallback
WEBSOCKET_REDIS_URL=redis://default:<password>@<host>:6379/0

# SMTP (optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=no-reply@booka.co.za

# Google OAuth / Maps (optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://api.booka.co.za/api/v1/google-calendar/callback

# Cloudflare R2 (optional, for media storage)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_S3_ENDPOINT=
R2_PUBLIC_BASE_URL=https://media.booka.co.za

