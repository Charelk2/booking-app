# Frontend local development env template

# Point the browser to your local API server
NEXT_PUBLIC_API_URL=http://localhost:8000

# WebSocket endpoint for realtime (local dev must be ws://)
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# Optional: Google Maps API key for local testing (leave blank if not needed)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Optional: allow direct image loads from your API origin while developing
NEXT_PUBLIC_MEDIA_HOSTS=localhost:8000

