# Frontend (Vercel) production env template

# Base URL of the FastAPI backend (Render/Railway/Fly)
NEXT_PUBLIC_API_URL=https://api.booka.co.za

# Optional explicit WebSocket URL; if unset, code derives ws(s) from API URL
# NEXT_PUBLIC_WS_URL=wss://api.booka.co.za

# Google Maps JS API key (browser)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Currency and receipt URLs (optional overrides)
# NEXT_PUBLIC_DEFAULT_CURRENCY=ZAR
# NEXT_PUBLIC_RECEIPT_BASE_URL=
# NEXT_PUBLIC_PAYFAST_RECEIPT_BASE_URL=

