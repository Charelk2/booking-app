SECRET_KEY=change_me_dev
FRONTEND_URL=http://localhost:3000
COOKIE_DOMAIN=

# Backend API origins for the frontend
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=http://localhost:8000

# Redis (optional but recommended)
REDIS_URL=redis://localhost:6379/0

# Postgres (via Cloud SQL Auth Proxy or local instance)
SQLALCHEMY_DATABASE_URL=postgresql+psycopg2://appuser:password@127.0.0.1:5432/appdb

# DB pool sizing (non-SQLite only)
DB_POOL_SIZE=15
DB_MAX_OVERFLOW=30
DB_POOL_RECYCLE=1800

# Paystack (test mode for dev)
PAYSTACK_SECRET_KEY=
PAYSTACK_PUBLIC_KEY=
# Frontend (Paystack Inline) public key
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=
# Optional: set to http://localhost:3000/payments/callback during local dev
PAYSTACK_CALLBACK_URL=
# Frontend flags
NEXT_PUBLIC_USE_PAYSTACK=1
NEXT_PUBLIC_FAKE_PAYMENTS=0
